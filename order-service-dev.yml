server:
  port: 9090 # 端口

spring:
  application:
    name: order-service  # 應用名稱

#配置Eureka Service 註冊中心
eureka:
  instance:
    prefer-ip-address: true     # 是否使用 ip 地址註冊
    instance-id: ${spring.cloud.client.ip-address}:${server.port} # ip:port
  client: 
    server-url:                 # 配置服務註冊中心地址
      defaultZone: http://localhost:8761/eureka/,http://localhost:8762/eureka/

# 自定義配置
name: order-service-dev-test-01
password: '{cipher}1b3515c77caf24eaf4be009dc52972f73dd557b6176c03a509ec3292c80cbafe'






